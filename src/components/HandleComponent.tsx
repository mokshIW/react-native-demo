import { View, Text } from "react-native";
import React from "react";

const HandleComponent = ({ handle }: { handle: string }) => {
  return <Text>Here is my insta handle: {handle}</Text>;
};

export default HandleComponent;
